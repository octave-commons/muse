---
category: "quality"
labels: ["regression", "ledger", "watch"]
write-id: "1786637898684-0.rc5izzqsthkj10cpmgm"
points: "2"
title: "Fix file-ledger watch-once event delivery"
priority: "P1"
status: "rejected"
uuid: "f856fdbd-e5a9-4229-80f7-183f098b2324"
created_at: "2026-08-13T16:10:00Z"
---

# Fix file-ledger watch-once event delivery

Fork-tax verification consistently exposed three failures in
`watch-once-resolves-on-append-test`: the watcher times out instead of
receiving the matching `wake.up` event.

## Acceptance

- Identify the actual watcher/store failure mechanism.
- Normalize mailbox and callback paths symmetrically across separators.
- Add focused callback-path coverage without weakening the end-to-end test.
- Full Shadow CLJS tests and clj-kondo pass with zero warnings.

---
Plan: trace watcher callback path and store identity, add focused path/callback regression coverage, fix the actual mechanism, then rerun the full gate.

Adjudication: rejected for this fork-tax scope. Exact-file watching exposed host EMFILE resource exhaustion; the prior recursive watcher silently converted its error into timeout. Review also found registration/truncation/error lifecycle semantics requiring a dedicated design. Source experiments were restored; full-suite watcher failure remains a visible blocker, not a false pass.
---