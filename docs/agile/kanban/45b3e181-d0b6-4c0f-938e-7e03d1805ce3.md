---
category: "kanban"
labels: "sandbox, build, review"
dependency: []
type: "task"
write-id: "1789199242017-0.e60ew3vj2yctwcughaw"
points: "2"
title: "Restore runnable Muse compilation and safe host wrappers"
priority: "P1"
status: "review"
uuid: "45b3e181-d0b6-4c0f-938e-7e03d1805ce3"
created_at: "2026-09-12T07:41:46.413Z"
---

# Restore runnable Muse compilation and safe host wrappers

## Outcome

All four Muse-owned build targets and the actual Node tests run from this checkout. Claude hook wrappers preserve an entry path containing whitespace, apostrophes or shell syntax as a single literal argument.

## Scope

- Retain the earlier cold-build repair and post-release hook generation.
- Replace the legacy non-UUID card muse-owned-build-targets with a UUIDv4 projection through Rheos; archive that historical identity without rewriting any prior ledger bytes.
- Quote generated hook entry paths and canonical event arguments at the Claude shell boundary.
- Add a regression that invokes generated wrapper bytes and verify the compiled emitter and resulting hook in an isolated path with spaces and apostrophes.

## Acceptance criteria

- Canonical card identity and filename are UUIDv4; the old identity is archived with explicit replacement provenance.
- Existing ledger bytes are an unchanged prefix after lawful Rheos comments and transitions.
- Generated wrappers deliver literal path/event arguments to Node and do not evaluate shell substitutions.
- The real emitted Claude wrapper accepts a hook request after the four-target build.
- Muse build, actual Node tests, and lint pass with zero warnings, failures or errors; remaining external review findings are handled by the PR owner.

---
PR16 Codex findings 3995536511 and 3995536512: this UUIDv4 card replaces the immutable legacy identity muse-owned-build-targets. Rheos create uses a title-based filename by default; renamed only the fresh projection filename to the repository-required UUIDv4.md before this recorded write. Implementation scope is literal shell argument quoting plus actual emitted-wrapper regression; earlier cold-build evidence remains historical.

Implemented both PR16 Codex findings. Literal POSIX shell words protect the actual compiled entry path and canonical event; regression invokes generated bytes with spaces, apostrophe, dollar-command-substitution and backticks. Four-target build passed daemon69/OpenCode119/MCP119/Claude121 with zero warnings in33.133s. Actual emitted Claude PreToolUse wrapper passed from an isolated unusual path; wrapper rejects the hook error fallback. Full actual Node tests ran with a native Mongo fixture:198tests517assertions, zero failures/errors, no unavailable-Mongo skip,22.584s. Full Clojure lint and JS syntax/lint passed0/0. All31208 pre-existing ledger bytes remain unchanged prefix; Rheos reports no drift. External review remains pending; machine-specific generated hook paths are not committed.

---