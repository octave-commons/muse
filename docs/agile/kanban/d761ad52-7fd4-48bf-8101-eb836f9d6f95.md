---
category: "infra"
labels: ["opencode", "submodule", "foresight"]
write-id: "1786628807747-0.9s6kjjekb7wdaji7212"
points: "1"
title: "Add forked opencode as a foresight submodule (reference)"
priority: "P1"
status: "review"
uuid: "d761ad52-7fd4-48bf-8101-eb836f9d6f95"
created_at: "2026-08-13T10:50:00Z"
---

# Add forked opencode as a foresight submodule (reference)

Depends on `0a21b52b` (fork exists). Add the fork to the foresight
meta-repo so the audit cards have the implementation on disk:

```
cd <foresight>
git submodule add --depth 1 git@github.com:open-hax/opencode.git opencode
```

## Acceptance

- `foresight/opencode` gitlink recorded in `.gitmodules` at the root.
- `.gitmodules` records `shallow = true` (reference, not a patch worktree).
- `nbb scripts/workspace.clj report` inventories it like other submodules.

---
Plan: add the verified open-hax fork to foresight as a shallow reference submodule, then verify .gitmodules, gitlink state, and default branch.

Verified parent .gitmodules records open-hax/opencode with shallow=true; local gitlink cc4b456 is shallow on dev; nbb report inventories opencode as initialized, clean, Bun-managed.
---